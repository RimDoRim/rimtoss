# 가상 투자 대결 사이트 - 진행 상황 정리 (2026-08-25)

친한 사람들끼리 하는 주식 단타 가상 대결 사이트. 회원가입은 관리자 승인제, 로그인은
이름+전화번호(비밀번호 없음). 토스증권 Open API로 실시간 시세를 받아 가상 계좌/주문/체결을
시뮬레이션한다.

## 아키텍처

```
토스증권 실시간 WS
      │
      ▼
toss-collector (FastAPI, :8000)         ─ 토스 WS 구독/재연결, Redis에 시세 저장·발행
      │  Redis (pub/sub + latest:* 캐시)
      ▼
distribution-server (FastAPI, :8100)    ─ 브라우저 WS로 시세 릴레이 (아직 프론트 없음)
      │
      └── Spring Boot toss (Gradle, Java 21, :8080)  ─ 회원/계좌/주문/체결 (Redis를 직접 읽음)
             │
             H2 (인메모리 - 재시작하면 다 날아감, 아직 영구 저장 아님)
```

세 서비스 모두 로컬에 실제 파일로 존재:
- `기림/toss-collector/`
- `기림/distribution-server/`
- `기림/toss/` (Spring Boot)

## 완성된 것

### toss-collector / distribution-server (Python, FastAPI)
- 토스 WS 구독(ref-count + 디바운스), 재연결, Redis pub/sub 발행 + `latest:{topic}` 스냅샷 캐싱
- distribution-server가 프론트용 WebSocket(`/ws`)으로 구독/스냅샷/실시간 갱신 릴레이
- **실제 토스 라이브 서버로 연결 테스트 완료** (삼성전자 005930 체결/호가 실시간 수신 확인)
- `websockets` 최신 버전(17.x)이라 `extra_headers` → `additional_headers`로 수정해서 씀
  (`toss-collector/app/ws_client.py`)

### Spring Boot `toss` 백엔드

패키지 구조 (`com.rim.toss` 하위):
```
common/       BaseTimeEntity(생성/수정일 자동기록), GlobalExceptionHandler
config/       SecurityConfig, CorsConfig, JpaConfig, RedisListenerConfig
member/       Member, MemberRole, MemberStatus, MemberRepository, MemberService,
              AuthController, AdminMemberController, AdminSeeder, dto/
account/      Account(현금잔고), Holding(보유종목), Repository들, dto/
order/        Order, OrderSide/Type/Status, Execution(체결기록), OrderService,
              OrderExecutionService(체결 로직), TradeTickListener, dto/
marketdata/   MarketDataService (Redis에서 시세 읽기), dto/(TradeSnapshot, OrderbookSnapshot, PriceLevel)
```

**인증 / 회원**
- 비밀번호 없음. 이름+전화번호 조합으로만 로그인 (`MemberService.authenticate`)
- 가입은 `PENDING`으로 시작 → 관리자가 `/api/admin/members/{id}/approve` 해야 로그인 가능
  (승인 시점에 시드머니 1천만원짜리 가상 계좌 자동 개설)
- 최초 관리자 계정은 `AdminSeeder`가 앱 시작 시 자동 생성 (ADMIN이 하나도 없을 때만)
  - **기본값 `관리자` / `01000000000` — 반드시 `ADMIN_NAME`/`ADMIN_PHONE_NUMBER` 환경변수로 바꿀 것**
- Spring Security: `/api/auth/signup`, `/api/auth/login`, `/h2-console/**`만 공개,
  `/api/admin/**`는 `ROLE_ADMIN`만, 나머지는 로그인 필요
- 전화번호는 평문 저장(관리자가 승인 화면에서 식별해야 해서). 폐쇄형 서비스라 의도적으로 가볍게 감.

**계좌 / 주문 / 체결**
- 지정가/시장가 주문 지원. 체결은 실제 상대 주문자가 없으니 distribution-server가 Redis에
  올리는 실시간 체결(trade) 틱을 상대로 함
- **부분체결 지원**: 한 번의 체결 시도에서는 "남은 주문 수량"과 "그 틱의 거래량(volume)" 중
  작은 쪽만 채움. 다 못 채우면 `PARTIALLY_FILLED`로 남아서 다음 틱을 기다림
  - 시장가: 가격 조건 없이 항상 매칭, 틱 거래량만큼만 채움 (자동 취소 안 함)
  - 지정가: 틱 가격이 조건 만족할 때만, 체결가는 항상 본인 지정가로 고정(가격 불리하게 체결 안 되게)
- `Execution` 엔티티로 체결마다 기록 남김 (수량/가격/시각). `GET /api/orders/{id}/executions`
  - `ExecutionRepository.findByMemberId()`를 이미 만들어둠 — "사용자별 로그" 기능을 나중에
    스키마 변경 없이 그대로 얹을 수 있음
- Redis pub/sub `trade:{market}:{symbol}` 채널을 직접 구독(`TradeTickListener`)해서 새 시세가
  올 때마다 대기 중인 지정가 주문을 재시도함
- 알려진 한계: 지정가 대기 주문에 현금/보유수량 에스크로(잠금)가 없음. 주문 시점에만 검사하고
  체결 시점엔 그때 잔고로 다시 검사함

**시세 스키마 (실제 토스 AsyncAPI 스펙으로 확정, 라이브 데이터로 검증 완료)**
```json
// trade: {"price":"259000","volume":"30","timestamp":"...","currency":"KRW"}
// orderbook: {"timestamp":"...","currency":"KRW","asks":[{"price":..,"volume":..}, ...], "bids":[...]}
```
asks는 낮은 가격순, bids는 높은 가격순. `currency`는 스펙이 "unknown enum 값 허용"을 요구해서
Java enum이 아니라 String으로 둠.

### 인프라 관련 이슈 (전부 해결됨)
- `build.gradle`의 `spring-boot-starter-session-data-redis` groupId 오타(`boort`) 수정
- Spring Boot 4는 Jackson 3 사용 — 패키지가 `com.fasterxml.jackson.*`가 아니라 `tools.jackson.*`
- Gradle 9.5.1에서 `./gradlew test`가 Windows에서 100% 실패하는 버그
  ([gradle/gradle#30304](https://github.com/gradle/gradle/issues/30304), 워커 classpath
  argfile을 잘못된 인코딩으로 씀) → **Gradle 9.7.1로 업그레이드해서 해결**

## 로컬에서 3개 서비스 같이 띄우기

```bash
# 0. Redis
docker run -d -p 6379:6379 redis:7

# 1. toss-collector (:8000) - .env에 실제 토스 client_id/secret 필요
cd toss-collector
.venv\Scripts\python.exe -m uvicorn app.main:app --port 8000

# 2. distribution-server (:8100)
cd distribution-server
.venv\Scripts\python.exe -m uvicorn app.main:app --port 8100

# 3. Spring Boot toss (:8080)
cd toss
./gradlew bootRun
```

시세를 받으려면 종목 구독을 걸어야 함: `POST http://localhost:8000/watch/kr/005930`
(직접 호출하거나, distribution-server의 `/ws`에 `{"action":"subscribe","market":"kr","symbol":"005930"}`
보내면 자동으로 collector에 watch가 걸림)

각 프로젝트의 `.env`는 `.env.example`을 복사해서 만들면 됨(둘 다 `.gitignore`에 걸려 있어서
git에는 안 올라감 — **토스 API 키는 이미 로컬 `.env`에만 있고 git에는 안 올라감, 확인 완료**).

## 다음에 할 일 (우선순위 순)

1. **DB 영구 저장** — 지금 H2 인메모리라 앱 재시작하면 회원/계좌/주문이 전부 날아감. 파일 기반
   H2로 최소한 바꾸거나, MySQL/Postgres로 갈지 결정 필요
2. **프론트엔드** — 지금 API만 있고 화면이 전혀 없음. 로그인/시세 확인/주문 화면 필요
3. **배포** — 친구들이 접속하려면 로컬 PC가 아니라 외부에서 접근 가능한 서버 필요
4. (여유 있으면) 사용자별 체결 로그 조회 API, 지정가 대기 주문 에스크로

## 참고 문서
- 토스 Open API 개요: https://openapi.tossinvest.com/openapi-docs/overview.md
- 토스 Open API AsyncAPI 스펙(WS 메시지 스키마 원본): https://openapi.tossinvest.com/openapi-docs/latest/asyncapi.json
- `toss/toss-collector.md`, `toss/distribution-server.md` — 두 파이썬 서비스의 코드 원본(마크다운),
  실제 파일은 `기림/toss-collector/`, `기림/distribution-server/`에 그대로 옮겨져 있음
